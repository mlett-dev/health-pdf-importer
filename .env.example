# Copy to .env for local overrides if a later story needs environment values.
# The importer is designed to run locally and must not require external APIs.

HEALTH_IMPORTER_CONFIG=config/app.yaml

# Required for productive Anytype MCP calls.
ANYTYPE_API_KEY=
ANYTYPE_FILES_DATA_DIR=~/ai/health-pdf-importer/data/
